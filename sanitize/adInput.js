/* eslint camelcase:0 */
module.exports = ({
  impressions = [],
  content,
  name,
  maxImpressions,
  advertiserId,
  adCampaigns = []
}) => ({
  impressions,
  content,
  name,
  maxImpressions,
  advertiserId,
  adCampaigns
})
